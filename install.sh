#!/usr/bin/env bash

cd "$(dirname "${BASH_SOURCE}")"

installTools() {
  # to install all necessary tools
}

backupConfig() {
  # to backup current config so it can be used later on anytime
}

installConfig() {

}
